import React from 'react';
import styled from 'styled-components';
let Radio = styled.input``;

const RadioBox = (props) => {
  return <Radio {...props} type='radio'></Radio>;
};

export default RadioBox;
